server:
  port: 8761
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
health:
  check:
    message: eureka service registry service is up now from git config-repo.
