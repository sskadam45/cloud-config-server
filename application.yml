spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
  application:
    name: BOOK-SERVICE
  flyway:
    baseline-on-migrate: true
  cloud:
    config:
      uri: http://localhost:8787

server:
  port: 8989

management:
  endpoints:
    web:
      exposure:
        include: "*"

health:
  check:
    message: book service is up now from git config-repo- changed  ravi...!!!

rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
